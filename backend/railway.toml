[build]
builder = "NIXPACKS"

[deploy]
# Migrations AVANT le démarrage (évite le timeout Railway ~2 min)
preDeployCommand = "npx prisma migrate deploy"
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
