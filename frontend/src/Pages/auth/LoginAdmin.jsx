// Legacy — ne plus utiliser. Login réel : components/auth/LoginAdmin.jsx
import { Navigate } from "react-router-dom";

const LoginAdmin = () => <Navigate to="/admin/login" replace />;

export default LoginAdmin;
